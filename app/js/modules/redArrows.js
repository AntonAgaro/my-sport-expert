
const redArrows = (arrowNextSelector, arrowPrevSelector, pointsSelector, pointActiveClass, textSelector, textActiveClass
  ) => {
  const next = document.querySelector(arrowNextSelector);
  const prev = document.querySelector(arrowPrevSelector);
  const points = document.querySelectorAll(pointsSelector);
  const texts = document.querySelectorAll(textSelector);

  const showPointAndText = (n) => {

  };
};

export default redArrows;